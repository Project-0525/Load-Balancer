# Beginner-Friendly Load Balancer

## Features
- Sticky IP mapping (same IP always routes to same node)
- Logging for each request
- Health check & fallback routing
- Rate limiting per IP
- Metrics dashboard

## Setup
```windows
git clone <your-repo-url>
cd load-balancer
npm install
npm start
